# Kotest Configuration
# Disable classpath scanning to avoid conflicts with :common:test module
kotest.framework.classpath.scanning.config.disable=true
kotest.framework.classpath.scanning.autoscan.disable=true
# Explicitly reference the Spring-aware config from :common:test
kotest.framework.config.fqn=io.averkhogliad.ai.challenge.common.test.SpringAwareKotestConfig
