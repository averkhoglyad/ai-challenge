services:
  events-server:
    build:
      context: ../..
      dockerfile: week-3/events-server/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - events-data:/data
volumes:
  events-data:
