weather:
  database:
    url: "jdbc:sqlite:${java.io.tmpdir}/weather-it-${random.uuid}.sqlite"
spring:
  main:
    allow-bean-definition-overriding: true
  test:
    context:
      bootstrapper: org.springframework.boot.test.context.SpringBootTestContextBootstrapper
