package io.averkhogliad.ai.challenge.week3.cli.application.handler

/**
 * Application-level action for the debug mode use case.
 */
enum class DebugAction {
    /** Toggle the current debug mode. */
    TOGGLE,

    /** Enable debug mode. */
    ON,

    /** Disable debug mode. */
    OFF
}
